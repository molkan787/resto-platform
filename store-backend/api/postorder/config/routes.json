{
  "routes": [
    {
      "method": "POST",
      "path": "/postorder",
      "handler": "postorder.createOrder",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/confirm_order_payment/:order_id",
      "handler": "postorder.confirmOrderPayment",
      "config": {
        "policies": []
      }
    }
  ]
}
